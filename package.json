{
  "name": "company-intelligence-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "Company intelligence MCP server — domain enrichment, sanctions screening, and beneficial ownership lookup for AI agents",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/main.js"
  },
  "dependencies": {
    "apify": "^3.6.0"
  }
}